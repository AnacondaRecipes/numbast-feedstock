anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: NVIDIA/numbast
      use_max_tag: true
      from_pattern: ^v?(\d+(?:\.\d+)*.*)$
      to_pattern: \1
    packages:
      - ast_canopy
      - numbast
